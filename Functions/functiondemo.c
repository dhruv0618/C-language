#include <stdio.h>

void sum()
{
    int a, b, sum = 0;
    printf("Enter two number: ");
    scanf("%d", &a);
    scanf("%d", &b);
    sum = a + b;
    printf("sum = %d \n", sum);
}
void machar()
{
    printf("M machar hu ....\n");
} 
{
    int subtract(int x, int y)
    return x - y;
}
int main()
{
    machar();
    sum();
    int a, b;
    printf("Enter two number: ");
    scanf("%d", &a);
    scanf("%d", &b);
    printf("Subtraction is : %d", subtract(a, b));
    return 0;
}
